#cloud-config
package_update: true
package_upgrade: true

packages:
  - qemu-guest-agent
  - openssh-server
  - curl
  - apt-transport-https
  - gpg
  - nfs-common

write_files:
  - path: /etc/sysctl.d/99-kubernetes-cri.conf
    content: |
      net.bridge.bridge-nf-call-iptables=1
      net.bridge.bridge-nf-call-ip6tables=1
      net.ipv4.ip_forward=1

runcmd:
  # --- 1. OS Setup ---
  # Load required networking kernel modules
  - modprobe overlay
  - modprobe br_netfilter
  - sysctl --system
  # Enable guest agent and ssh
  - systemctl enable qemu-guest-agent
  - systemctl enable ssh

  # --- 2. Install Container Runtime (Containerd) ---
  - apt-get update
  - apt-get install -y containerd
  - mkdir -p /etc/containerd
  - containerd config default > /etc/containerd/config.toml
  - sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
  - systemctl restart containerd

  # --- 3. Setup Kubernetes Repositories ---
  # Download the public signing key for the Kubernetes package repositories
  - curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
  # Add the appropriate Kubernetes repository
  - echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list
  # --- Install required packages
  - apt-get update
  - apt-get install -y kubelet kubeadm kubectl
  - apt-mark hold kubelet kubeadm kubectl

  # --- Kubernetes Cluster Join (CRITICAL STEP) ---
  # Replace the entire line below with the output from your master's 'kubeadm token create' command.
  #- kubeadm join 192.168.1.10:6443 --token abcdef.0123456789abcdef --discovery-token-ca-cert-hash sha256:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

  # --- Finalize ---
  - systemctl enable kubelet
  - reboot

final_message: "Kubernetes Worker Node setup complete."
